README for HDDAQ

Last updated: 2015.03.25 by K. Hosomi

INSTALL:
  $cd hddaq
  $make

  %%% for compiling Frontend %%%
  $cd Frontend/XXX_node
  $make

  %%% making work directory %%%
  $mkdir /home/sks/daq
  $cd daq
  $ln -s <path to hddaq>/script/launcher.py .
  $ln -s <path to hddaq>/script/kill.sh . 
  $cp <path to hddaq>/misc/msgnode.txt .
  $cp <path to hddaq>/misc/datanode.txt .

  #if nessesarry,
  $ln -s <path to data storage> data


RUNNING:
  %%% for Frontend %%%%
  $cd <path to hddaq>/Frontend/script
  #change nickname, nodeid, frontend values in frontend.sh script depending on your frontend
  
  #start frontend
  $sh fe_start.sh
  
  #kill frontend
  $sh fe_kill.sh


  %%% for Main DAQ tools %%%
  $cd <path to work directory>
  $sh launcher.py <path to data storage>
  #use GUI tools
  
  #kill main
  $sh kill.sh